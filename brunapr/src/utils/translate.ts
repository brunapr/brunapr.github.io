export default function translate(text: string) {
  const language = "PT-BR";

  if(language == "PT-BR") {
    switch (text) {
      case "":
    }
  } else if(language == "ENG") {
    switch (text) {
      case "":
    }
  }
}